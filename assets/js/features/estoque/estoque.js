/* Estoque unificado: materiais + alertas. A tabela materiais continua no banco para preservar dados. */
async function initEstoque(){
  layout();const p=await shell('Estoque');if(!p)return;const u=await user();
  document.getElementById('content').innerHTML=`
    <section class="dashboard-hero modern-hero"><div><span class="eyebrow">CONTROLE DE ESTOQUE</span><h2>Seus materiais em um só lugar.</h2><p>Cadastre insumos, acompanhe quantidades e veja rapidamente o que precisa ser reposto.</p></div><a class="btn btn-primary" href="#novo-material">+ Novo material</a></section>
    <div class="metric-grid stock-metrics"><div class="metric-card"><div class="metric-icon">▤</div><div><small>Total de materiais</small><strong id="estoqueTotal">0</strong></div></div><div class="metric-card"><div class="metric-icon">!</div><div><small>Precisam de reposição</small><strong id="estoqueBaixo">0</strong></div></div><div class="metric-card"><div class="metric-icon">R$</div><div><small>Valor estimado</small><strong id="estoqueValor">R$ 0,00</strong></div></div></div>
    <div class="card" id="novo-material"><div class="panel-heading"><div><span class="panel-kicker">INVENTÁRIO</span><h3>Cadastrar material</h3></div></div><form id="materialForm" class="grid grid-3"><div><label class="label">Material *</label><input class="input" name="nome" required></div><div><label class="label">Categoria</label><input class="input" name="categoria"></div><div><label class="label">Unidade</label><select class="select" name="unidade"><option>unidade</option><option>grama</option><option>metro</option><option>rolo</option><option>caixa</option><option>pacote</option></select></div><div><label class="label">Quantidade</label><input class="input" type="number" name="quantidade" min="0" step="0.001" value="0"></div><div><label class="label">Estoque mínimo</label><input class="input" type="number" name="estoque_minimo" min="0" step="0.001" value="0"></div><div><label class="label">Custo unitário</label><input class="input" type="number" name="custo_unitario" min="0" step="0.01" value="0"></div><div><label class="label">Fornecedor</label><input class="input" name="fornecedor"></div><div style="grid-column:1/-1"><label class="label">Observações</label><textarea class="input" name="observacoes" rows="3"></textarea></div><div class="actions"><button class="btn btn-primary">Salvar material</button></div></form></div>
    <div class="card"><div class="panel-heading"><div><span class="panel-kicker">INVENTÁRIO</span><h3>Materiais cadastrados</h3></div><div class="filter-inline"><input class="input" id="estoqueBusca" placeholder="Buscar material..."></div></div><div class="table-wrap"><table class="table"><thead><tr><th>Material</th><th>Categoria</th><th>Quantidade</th><th>Custo unit.</th><th>Status</th><th>Ações</th></tr></thead><tbody id="estoqueList"></tbody></table></div></div>`;
  async function load(){const {data,error}=await supabaseClient.from('materiais').select('*').eq('usuario_id',u.id).order('nome');if(error){msg(error.message,false);return}const low=(data||[]).filter(m=>Number(m.quantidade)<=Number(m.estoque_minimo));document.getElementById('estoqueTotal').textContent=(data||[]).length;document.getElementById('estoqueBaixo').textContent=low.length;document.getElementById('estoqueValor').textContent=money((data||[]).reduce((s,m)=>s+Number(m.quantidade)*Number(m.custo_unitario),0));const render=()=>{const term=String(document.getElementById('estoqueBusca').value||'').toLowerCase();document.getElementById('estoqueList').innerHTML=(data||[]).filter(m=>String(m.nome).toLowerCase().includes(term)||String(m.categoria||'').toLowerCase().includes(term)).map(m=>{const l=Number(m.quantidade)<=Number(m.estoque_minimo);return `<tr><td><b>${esc(m.nome)}</b><small class="muted">${esc(m.fornecedor||'')}</small></td><td>${esc(m.categoria||'—')}</td><td>${Number(m.quantidade)} ${esc(m.unidade)}</td><td>${money(m.custo_unitario)}</td><td><span class="pill ${l?'danger':'success'}">${l?'Repor':'Normal'}</span></td><td><button class="btn btn-danger btn-sm" data-delete="${m.id}">Excluir</button></td></tr>`}).join('')||'<tr><td colspan="6" class="muted">Nenhum material encontrado.</td></tr>';document.querySelectorAll('[data-delete]').forEach(b=>b.onclick=async()=>{if(!confirm('Excluir este material?'))return;const {error}=await supabaseClient.from('materiais').delete().eq('id',b.dataset.delete).eq('usuario_id',u.id);if(error)msg(error.message,false);else load()})};render();document.getElementById('estoqueBusca').oninput=render}
  document.getElementById('materialForm').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);const payload={usuario_id:u.id,nome:String(f.get('nome')).trim(),categoria:String(f.get('categoria')||'').trim()||null,unidade:f.get('unidade'),quantidade:Number(f.get('quantidade')||0),estoque_minimo:Number(f.get('estoque_minimo')||0),custo_unitario:Number(f.get('custo_unitario')||0),fornecedor:String(f.get('fornecedor')||'').trim()||null,observacoes:String(f.get('observacoes')||'').trim()||null};const {error}=await supabaseClient.from('materiais').insert(payload);if(error)msg(error.message,false);else{msg('Material cadastrado!');e.target.reset();load()}};load();
}